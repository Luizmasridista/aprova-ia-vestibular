import React from 'react';
import type { DashboardStats } from '@/hooks/useDashboardData';

// (Removido: estilos e UI do dashboard)

// (Removido: DifficultyBadge)

// (Removido: formatDate)

interface SubjectPerformanceChartProps {
  stats: DashboardStats;
  isLoading: boolean;
}

// (Removido: tipos auxiliares não usados)

export const SubjectPerformanceChart: React.FC<SubjectPerformanceChartProps> = () => {
  return null;
};

// (Removido: lógicas auxiliares e retorno do dashboard)

export default SubjectPerformanceChart;